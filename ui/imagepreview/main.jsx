import React from 'react';
import ReactDOM from 'react-dom/client';
import ImagePreview from './ImagePreview';
import '../src/styles/App.css';

ReactDOM.createRoot(document.getElementById('root')).render(<ImagePreview />);
