import React from 'react';
import ReactDOM from 'react-dom/client';
import WorkspacePopup from './WorkspaceSwitcher';
import '../src/styles/App.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <WorkspacePopup />
  </React.StrictMode>
);
